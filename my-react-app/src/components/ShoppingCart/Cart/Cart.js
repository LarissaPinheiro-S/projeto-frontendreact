import Items from "../Items/Items";
import React from "react";

function Cart({
  amount,
  setAmount,
  cart,
  setCart
}) {
    return (
      <div>
       <Items/>
      </div>
    );
  }
  export default Cart;